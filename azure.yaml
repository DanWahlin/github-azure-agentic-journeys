name: n8n-azure
metadata:
  template: n8n-azure-container-apps

infra:
  provider: bicep
  path: infra-grafana

hooks:
  postprovision:
    posix:
      shell: sh
      run: ./infra-grafana/hooks/postprovision.sh
    windows:
      shell: pwsh
      run: ./infra-grafana/hooks/postprovision.ps1
